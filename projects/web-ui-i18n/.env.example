# Base URL of the Spring API. The Next.js Route Handlers under
# app/api/i18n/translations/ proxy admin requests to this host.
# Used server-side only — never bundled into the client.
API_BASE_URL=http://localhost:8080
