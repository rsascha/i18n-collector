apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - namespace.yaml
  - ../../base
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/environment: prod
